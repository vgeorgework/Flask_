apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask_helm.fullname" . }}
  labels:
    {{- include "flask_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask_helm.selectorLabels" . | nindent 4 }}
